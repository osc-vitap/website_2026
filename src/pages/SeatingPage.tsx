import SeatingLayout from '../components/SeatingLayout';

export default function SeatingPage() {
  return (
    <div className="w-full bg-[#0b0b0d] min-h-screen font-sans" style={{ fontFamily: "'Objectivity', 'SF Pro Display', -apple-system, sans-serif" }}>
      <SeatingLayout />
    </div>
  );
}
